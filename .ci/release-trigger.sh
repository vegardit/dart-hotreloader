# this file is evaluated by .ci/build.sh

 RELEASE_VERSION="3.0.4" # if version pubspec.yaml contains this version, then perform a release
NEXT_DEV_VERSION="3.0.5-SNAPSHOT" # increment to this dev version
